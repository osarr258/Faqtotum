"""Security package — cross-cutting concerns.

Modules
-------
- dependencies: FastAPI DI (get_current_user, require_roles, require_owner)
- ownership:    resource-level ownership checks (per resource type)
"""
